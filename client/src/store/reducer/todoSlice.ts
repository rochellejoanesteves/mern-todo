import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    _id: "",
    name: ""
};

const todoSlice = createSlice({
    name: "todoSlice",
    initialState,
    reducers : {
        
    }
})


